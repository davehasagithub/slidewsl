# This file is auto-generated. Any changes made to this file will be lost.
# Generated with the command: go run render.go --env-file /home/dave/slidewsl/_env/build.env --environment build --workspace-path /home/dave/slidewsl

name: slidewsl-build

networks:
  php-fpm:
  keydb:
  mysql:
  angular:
  builder-net:

volumes:
  keydb:
  yarn-cache:
  mysql:

services:

  build:
    image: ${REGISTRY-}deploy-build:${BUILD_TAG}
    deploy:
      replicas: 0
    volumes:
      - /etc/localtime:/etc/localtime:ro
    build:
      context: node
      additional_contexts:
        angular: ${SLIDEWSL_ANGULAR_ROOT_IN_WSL}
        laravel: ${SLIDEWSL_LARAVEL_ROOT_IN_WSL}
        webroot: ${SLIDEWSL_WEB_ROOT_IN_WSL}
      target: build
      args:
        - APP_NAME=${APP_NAME:-undefined}
        - NODE_VERSION=$NODE_VERSION
        - NG_CLI_VERSION=$NG_CLI_VERSION
        - BUILD_IMAGE=deploy-build:$BUILD_TAG

  angular-ssr:
    image: ${REGISTRY-}deploy-angular-ssr:${BUILD_TAG}
    hostname: angular-ssr
    networks:
      - angular
    volumes:
      - /etc/localtime:/etc/localtime:ro
    build:
      context: node
      target: angular-ssr
      args:
        - NODE_VERSION=$NODE_VERSION
        - NG_CLI_VERSION=$NG_CLI_VERSION
        - APP_NAME=${APP_NAME:-undefined}
        - BUILD_IMAGE=deploy-build:$BUILD_TAG

  nginx:
    image: ${REGISTRY-}deploy-nginx:${BUILD_TAG}
    hostname: nginx
    environment:
      EXTERNAL_WEB_PORT: ${EXTERNAL_WEB_PORT:-443}
    ports:
      - "0.0.0.0:${NGINX_SECURE_PORT:-443}:4430"
    networks:
      - php-fpm
      - angular
    volumes:
      - /etc/localtime:/etc/localtime:ro
    depends_on:
      - angular-ssr
      - php-fpm
    build:
      context: nginx
      additional_contexts:
        shared: shared
      target: deployable
      args:
        - BUILD_IMAGE=deploy-build:$BUILD_TAG

  php-fpm:
    image: ${REGISTRY-}deploy-php-fpm:${BUILD_TAG}
    hostname: php-fpm
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      PHP_INI_SCAN_DIR: /usr/local/etc/php/conf.d:/etc/php/conf.d
    networks:
      - php-fpm
      - keydb
      - mysql
    depends_on:
      - keydb-node1
      - mysql
    build:
      context: php
      additional_contexts:
        shared: shared
      args:
        - PHP_VERSION=$PHP_VERSION
        - PHP_REDIS_VERSION=$PHP_REDIS_VERSION
        - CONF=conf
        - BUILD_IMAGE=deploy-build:$BUILD_TAG
      target: deployable

  mysql:
    image: ${REGISTRY-}deploy-mysql:${BUILD_TAG}
    hostname: mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
    networks:
      - mysql
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - mysql:/var/lib/mysql
    build:
      context: mysql
      additional_contexts:
        shared: shared
      target: deployable
      args:
        - CONF=conf/docker.cnf

  phpmyadmin:
    image: ${REGISTRY-}deploy-phpmyadmin:${BUILD_TAG}
    hostname: phpmyadmin
    environment:
      PMA_HOST: mysql
    networks:
      - mysql
    volumes:
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "0.0.0.0:${PHPMYADMIN_PORT:-8080}:80"
    build:
      context: phpmyadmin
      additional_contexts:
        shared: shared
      target: deployable
      args:
        - CONF=conf.d
    depends_on:
      - mysql

  keydb-node1:
    image: ${REGISTRY-}deploy-keydb-node1:${BUILD_TAG}
    hostname: keydb-node1
    networks:
      - keydb
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - keydb:/data
    build:
      context: keydb
      additional_contexts:
        shared: shared
      target: deployable
      args:
        - CONF=keydb-node1/keydb.conf

  keydb-node2:
    image: ${REGISTRY-}deploy-keydb-node2:${BUILD_TAG}
    hostname: keydb-node2
    networks:
      - keydb
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - keydb:/data
    build:
      context: keydb
      additional_contexts:
        shared: shared
      target: deployable
      args:
        - CONF=keydb-node2/keydb.conf

  keydb-node3:
    image: ${REGISTRY-}deploy-keydb-node3:${BUILD_TAG}
    hostname: keydb-node3
    networks:
      - keydb
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - keydb:/data
    build:
      context: keydb
      additional_contexts:
        shared: shared
      target: deployable
      args:
        - CONF=keydb-node3/keydb.conf

