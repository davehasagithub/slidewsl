DOCKER_HOST=tcp://127.0.0.1:2376
COMPOSE_FILE=/docker/compose.yaml
USER=${USER}
WSL_UID=${WSL_UID}
WSL_GID=${WSL_GID}
TERM=${TERM}
WSL2_GATEWAY=${WSL2_GATEWAY}

# these can be overridden using .env.devcontainer
SLIDEWSL_ANGULAR_ROOT_IN_WSL=${HOME}/src/angular
SLIDEWSL_LARAVEL_ROOT_IN_WSL=${HOME}/src/laravel
SLIDEWSL_WEB_ROOT_IN_WSL=${HOME}/src/web
SLIDEWSL_DB_ROOT_IN_WSL=${HOME}/db
